import survey, { surveyQuestion, surveyResponse, surveyAnswer } from "./survey";

export { survey, surveyQuestion, surveyResponse, surveyAnswer };

export default [survey, surveyQuestion, surveyResponse, surveyAnswer];
